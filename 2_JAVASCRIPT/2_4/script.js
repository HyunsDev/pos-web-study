// 파이썬의 input()와 비슷한 prompt() 함수
const promptResponse = prompt('내용')
console.log(promptResponse)

// 참 / 거짓을 요청하는 confirm() 함수
const confirmResponse = confirm('참/거짓')
console.log(confirmResponse)

// 단순히 화면에 알림 표시
alert("알림")